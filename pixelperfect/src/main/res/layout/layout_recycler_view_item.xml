<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:fitsSystemWindows="true"
    android:layout_margin="5dp">

    <FrameLayout
        android:id="@+id/recycler_item_image_container_frame_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/bg_settings_image_border">

        <ImageView
            android:id="@+id/recycler_item_image_view"
            android:layout_width="90dp"
            android:layout_height="150dp"
            android:scaleType="centerCrop"
            android:layout_margin="2dp"/>
    </FrameLayout>

    <TextView
        android:id="@+id/recycler_item_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:background="@color/black_70_alpha"
        android:gravity="center"
        android:minWidth="100dp"
        android:padding="4dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:textColor="@color/sl_image_name"/>
</FrameLayout>